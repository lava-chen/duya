/** Wire names for the bot routine self-management tool (Plan 476 P2.3b). */
export const MANAGE_ROUTINE_TOOL_NAME = 'manage_routine';

/** Upper bound on routines one bot may own (mirrors AUTOMATION_MAX_PER_AGENT). */
export const MAX_ROUTINES_PER_BOT = 50;
