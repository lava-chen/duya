/**
 * packages/agent/src/tool/DuyaCliTool/constants.ts
 *
 * Stable tool name. The CLI control plane is the single source of
 * truth for the agent's self-inspection and reversible management
 * actions; this tool is the *only* way the agent invokes it.
 */
export const DUYA_CLI_TOOL_NAME = 'duya_cli';
