{
  "servers": [
    {
      "name": "github",
      "command": "github-mcp-server",
      "args": ["stdio"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${setup.githubToken}"
      },
      "envPassthrough": "allowlist",
      "_status": "official-stdio",
      "_authentication": "Prefer GitHub App installation tokens or OAuth tokens; long-lived PATs are discouraged."
    }
  ]
}
