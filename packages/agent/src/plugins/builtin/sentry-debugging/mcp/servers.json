{
  "servers": [
    {
      "name": "sentry",
      "command": "npx",
      "args": [
        "-y",
        "@sentry/mcp-server"
      ],
      "env": {
        "SENTRY_AUTH_TOKEN": "${setup.sentryAuthToken}",
        "SENTRY_ORG_SLUG": "${setup.sentryOrgSlug}"
      },
      "envPassthrough": "allowlist",
      "_status": "transitional",
      "_migration": "Will migrate to the official Sentry Remote MCP endpoint (https://mcp.sentry.dev) once Plan 313 Phase 2a lands the HTTP transport. Until then the stdio fallback wraps @sentry/mcp-server. The auth token will be exchanged for an OAuth flow managed by Sentry once the remote endpoint lands."
    }
  ]
}
