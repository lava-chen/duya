{
  "servers": [
    {
      "name": "linear",
      "command": "npx",
      "args": ["-y", "@tacticlaunch/mcp-linear"],
      "env": {
        "LINEAR_API_KEY": "${setup.linearApiKey}"
      },
      "envPassthrough": "allowlist",
      "_status": "transitional",
      "_migration": "Will migrate to the official Linear Remote MCP endpoint (https://mcp.linear.app/sse) once Plan 313 Phase 2a lands the HTTP transport. Until then the stdio fallback wraps a Linear MCP adapter. The personal API key will be exchanged for an OAuth flow managed by Linear once the remote endpoint lands."
    }
  ]
}
