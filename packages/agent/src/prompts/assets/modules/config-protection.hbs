# Config file protection

Do NOT modify `~/.duya/config.toml` or `~/.duya/secrets.json` directly — use `duya_cli` or the desktop Settings panel instead. Direct edits bypass ConfigStore validation and the audit log.
