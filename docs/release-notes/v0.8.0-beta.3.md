- fix(channels): move bot channel data to the shared ~/.duya/agents root (plan 526) — packaged installs now share bindings/secrets with dev, channels connect without reconfig; boot migration merges legacy userData data
- feat(agent): guard against degenerate compaction summaries — degenerate exhaustion throws, tool-marker blacklist, structural signals, tools disabled for summary requests (plan 523)
- feat(agent): four-tier tool exposure with hint stubs and appended schema catalog (plan 480)
- feat(browser): X/Twitter post action with platform capability guides
- feat(agent): runtime context payload, per-message call usage and message projectors
- feat(electron): per-model usage pricing (cache-write 1h TTL, reasoning tokens) and cache-waste updates
- feat(electron): lazy-load IPC handler groups and ship builtin plugins via extraResources
- feat(ui): renderer perf (useShallow), plugin mention rendering, context usage display
- feat(ai): model catalog sync script and provider request handling updates
