- Polish the chat command popover recap view with Chinese copy and inline
  recap rendering.
- Treat selected skill chips in the input as atomic controls for deletion.
- Preserve provider/model pairing when changing chat models and improve
  transient mailbox/context UI handling.
- Include minimal-mode allowlist and release verification fixes merged from
  the beta.6 branch.
