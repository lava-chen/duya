- Fix the Release workflow's `npm ci` blocker by synchronizing the lockfile
  with the root `gifenc` dependency.
- Preserve the macOS arm64 release path introduced in beta.4; this release
  reaches packaging so the matrix can validate it.
- Fix minimal MessageSession mode to pass lowercase `read`, `grep`, and
  `glob` registry names, restoring its read-only toolset.
- Emit a warning when a caller-supplied tool allowlist matches no registered
  tools, so future registry-name regressions are diagnosable at the target
  worker.
