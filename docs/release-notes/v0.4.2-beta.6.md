- Verify both macOS arm64 and Intel x64 packaged apps against their target
  architecture instead of comparing every artifact to the Apple Silicon CI
  host.
- Keep the dual-architecture macOS release configuration explicit; afterPack
  rebuilds `better-sqlite3` separately for each target.
