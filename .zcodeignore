# Dependencies
node_modules/

# Build outputs
.next/
dist/
dist-electron/
release*/
build/
*.tsbuildinfo

# Downloaded ripgrep binary (scripts/fetch-ripgrep.mjs) — fetched at build
# time, never committed.
resources/ripgrep/

# Generated assembled prompt docs (read-only local reference, not source)
/doya-general-prompt-assembled.md

# In-place TypeScript emit (when tsc is invoked with emit on, .d.ts and
# .js appear next to their .ts / .tsx sources). Track emit outputs by
# filename pattern rather than path so genuine hand-written .d.ts
# files (e.g. src/global.d.ts, src/vite-env.d.ts) stay tracked.
src/**/*.d.ts
src/**/*.d.ts.map
src/**/*.js
src/**/*.js.map
!src/**/*.stories.tsx
!src/**/*.stories.ts

# Same in-place emit, but for workspace packages and electron/ (no tracked
# hand-written .js exists there — all source is TS).
electron/**/*.js
electron/**/*.js.map
electron/**/*.d.ts
electron/**/*.d.ts.map
packages/*/src/**/*.js
packages/*/src/**/*.js.map
packages/*/src/**/*.d.ts
packages/*/src/**/*.d.ts.map

# Storybook
.storybook/
storybook-static/
src/stories/
# Co-located story files (Storybook v8+ convention) — not committed
*.stories.tsx
*.stories.ts
**/__stories__/

# Environment files
.env
.env.local
.env.*.local

# Runtime data
.duya/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Coverage
coverage/

# Unit tests are tracked like any other source (colocated *.test.ts /
# *.spec.ts, e2e/*.spec.ts). No ignore rules for them: the previous
# blanket *.test.ts rule silently dropped every new test file unless it
# was individually negated below.

# Project specific
ui-screenshot-*.png
console_log.txt
test_permissions.txt
test_paper.pdf
duyaUI.pen

# Agent tools
.claude/
.trae/
.qoder/
.mimosa/
.playwright-mcp/
.playwright-cli/
.e2e-userdata/
tmp/

# Scratch debug / probe scripts (do not commit)
scripts/_probe-*.mjs
scripts/_probe-*.js
scripts/_scratch-*.mjs
scripts/_scratch-*.js
scripts/probe-*.cjs
test-results/

# Docs (keep only product-specs and release-notes)
docs/*/
!docs/product-specs/
!docs/release-notes/
docs/*.md

# Tests
# Tests: only the root-level `tests/` scratch directory is ignored.
# `packages/agent/tests/` and `packages/plugin-core/tests/` are intentionally
# tracked — they hold the project's unit-test source.
/tests/

# Bundled output
packages/agent/bundle/
packages/gateway/bundle/
packages/cli/bundle/

# Bundled output


.cache/

# Temporary validation / probe scratch directories
.tmp-validation/
.tmp-test/
.tmp-electron-dev*.log
.tmp-electron-dev2*.log
.tmp-electron-dev3*.log
.tmp-electron-dev4*.log
.tmp-electron-dev5*.log
.tmp-electron-dev6*.log

# Local scratch / debug artifacts
_dbg-out.txt
.codex-artifacts/

# Local codegraph index
.codegraph/

# Local zcode / introspection index
.zcode/

# Ad-hoc scratch scripts left in repo root
/test-*.mjs
/*.cjs
/tc.out
/tc.out2
/diag.js
/fix-*.js

# Showcase / launch screenshot artifacts (regenerated by `npm run showcase`)
assets/showcase/

# Playwright scratch screenshots and ad-hoc agent/build output
output/

# Brainstorm visual companion scratch
.superpowers/

# Transient agent-session backups and scratch staging (never commit)
.duya-backup-*
.duya-stage/
.serena/

# Local ABI prebuilt artifacts + stash patch archives (machine-specific)
prebuilds/
.stash-archive/

# Voice / desktop probe artifacts (debug scratch, never commit)
voice-mic-*.ps1
voice-mic-probe.wav
voice-mic-probes/
probe-desktop-capture*.png
.tmp-activate-wechat.ps1

# WorkBuddy project data (local memory, not source)
.workbuddy/
.tmp-diag.ps1
.tmp-kill.ps1

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
bower_components/
jspm_packages/
__pycache__/
site-packages/
venv/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
playwright-report/
allure-results/
allure-report/
cdk.out/
*.egg-info/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
